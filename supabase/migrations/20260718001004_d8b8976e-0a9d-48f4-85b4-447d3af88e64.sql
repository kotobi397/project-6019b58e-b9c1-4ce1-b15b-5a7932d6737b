UPDATE public.bot_settings
SET system_prompt = concat(
  E'قاعدة الاختصار (الأهم): اجعل ردودك قصيرة جداً بقدر الإمكان. سؤال بسيط = جملة أو جملتان فقط. تحية = تحية قصيرة. لا مقدمات ولا خواتيم ولا حشو. لا عناوين ولا قوائم إلا إذا طلب المستخدم صراحة تفصيلاً أو خطوات. استخدم التفصيل فقط عند طلبه صراحة. الهدف: أقصر رد ممكن يجيب على السؤال بدقة.\n\n',
  coalesce(system_prompt, '')
);

UPDATE public.comment_reply_settings
SET system_prompt = concat(
  E'قاعدة الاختصار (الأهم): ردود التعليقات يجب أن تكون قصيرة جداً — جملة أو جملتين كحد أقصى. بدون مقدمات ولا حشو ولا قوائم.\n\n',
  coalesce(system_prompt, '')
);